pseudocode 

1. prompt the user to enter a latter
2. collect the later
3. store the latter as alphabet 
4. if alphabet ='A' || 'E' || 'I' || 'O' || 'U' || 'a' || 'e' || 'i' || 'o' || 'u' ) display  its vowel
5.  else if alphabet = 'B' || 'b' || 'C' || 'c' || 'D' || 'd' || 'F' || 'f' || 'G' || 'g' || 'H' || 'h' || 'J' || 'j' || 'K' || 'k' || 'L' || 'l' || 'M' || 'm' || 'N' || 'n' || 'P' || 'p' || 'Q' || 'q' || 'R' || 'r' || 'S' || 's' || 'T' || 't' || 'V' || 'v' || 'W' || 'w' || 'X' || 'x' || 'Y' || 'y' || 'Z' || ' z ' ) Display is consonant 
6. else display error message 