elementpseudocode 
declare as integer
create a new array numberslist
declare minimumElement
initialize  minimumElementt to 0
iterate through each element  
prompt the user to Enter a number 
collect the number 
store through numberslist(element)
if the user input is less than 1
	assign numberlist[0] to the minimumElement
if user input less then numberlist[0] 
	assign as minimumElement
display minimumElement

