pseudocode

initialize counter to zero
firstlargest counter to zero
secondlargest counter to zero
while counter less then 10 
	prompt user to enter a number 
  	collect number 
	store as number
	if number > firstlargest
	store first largest as number
	if number > secondlargest && number != firstlargest
	store as second largest 
display first largest 	
display second largest 
